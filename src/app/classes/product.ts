export class Product {
    constructor(public id: number, public name: string, public description: string, public price: number, public unity: string, public decimal: number, public tags: string[]) {
    }
}
