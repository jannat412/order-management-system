export interface IUser {
    name: string;
    secondName: string;
    role: string;
    address?: string;
    city?: string;
    cp?: string;
    district?: string;
    tel?: number;
    email: string;
}
